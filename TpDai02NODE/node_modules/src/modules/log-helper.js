export default function CatchErrores(text) {

    const fs = require('fs');

    const content = `${text}`;

    fs.writeFile('/Users/joe/test.txt', content, err => {
        if (err) {
            console.error(err);
        }
        // file written successfully
    });

}